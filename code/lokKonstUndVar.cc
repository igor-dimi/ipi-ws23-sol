int f( int x )
{
  const int a = 3; // Konstante, kann nicht geaendert werden

  int b = x;       // Variablendefinition mit Initialisierung
  
  int c;           // Variablendefinition ohne Initialisierung
}
